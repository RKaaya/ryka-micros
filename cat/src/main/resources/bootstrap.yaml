server:
  port: 9001

spring:
  application:
    name: cat
  cloud:
    config:
      uri: http://localhost:9000/

#springdoc:
#  swagger-ui:
#    path: "/swagger-ui-custom.html"

#Don't hardcode it
encrypt:
  key: secret1234

info:
  app:
    name: Cat Service
    description: Simple REST service
    version: 0.0.1